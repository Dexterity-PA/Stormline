# scrapers/

Standalone Python scrapers for non-API data sources.
Same pattern as BidBoard: httpx + selectolax.

Each scraper is a self-contained folder with its own venv/requirements.
Not part of the Next.js build.
